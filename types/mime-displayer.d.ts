import { MimeComponentProps } from './mime-component-props';

/**
 * Type of the a react component that displays mime type props
 */
export type MimeDisplayer = (props: MimeComponentProps) => JSX.Element;
